# Replicate (one of the following token vars is required; first non-empty is used)
REPLICATE_API_TOKEN=
# REPLICATE_API_KEY=
# REPLICATE_TOKEN=

# Configure the model either by version OR by owner/name (prefer owner/name to auto-track latest)
# If you know the exact version id, set it here and leave owner/name unset:
# REPLICATE_MODEL_VERSION=

# Or specify the model slug:
# REPLICATE_MODEL_OWNER=owner
# REPLICATE_MODEL_NAME=model